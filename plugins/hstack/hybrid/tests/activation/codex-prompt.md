Use **hstack** poteto-mode and its Codex runtime adapter to fix the refund defect
in the supplied disposable fixture. Record the package identity/source version
and actual loaded skill/runtime paths. State whether native plugin discovery or
explicit staged-file loading was used; do not claim one proves the other. If
hstack is absent, report that activation blocker. Preserve model preferences.

Read the README, reproduce the four CLI acceptance tests with three failures,
and record BASE. Implement the smallest correct fix without weakening tests, run
the real sample CLI and all four tests, and commit locally. Do not push, merge or
modify the main project. Record HEAD and final clean Git status.

Use available native Codex delegation for an independent read-only review of
BASE..HEAD, including the requirements and acceptance command. Record its real
evidence. If native delegation is unavailable, explicitly mark independent review
unverified; do not invent a tool or launch a paid nested CLI as a substitute.

Return loaded hstack identity, capability limitations, BASE/HEAD, diff, sample
output, acceptance results and review evidence. Preserve evidence before the VM
ends. Direct Codex cloud use does not establish the separate Cursor-to-Codex CLI
connection.
